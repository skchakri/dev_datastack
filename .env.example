# Environment Variables Template
# Copy this file to .env and customize the values for your setup
# The bootstrap.sh script will help you set these interactively

# MySQL Configuration
MYSQL_USER=root
MYSQL_ROOT_PASSWORD=your_secure_password_here

# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here

# MongoDB Configuration
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=your_secure_password_here

# pgAdmin Configuration (PostgreSQL Web UI)
PGADMIN_DEFAULT_EMAIL=admin@yourdomain.com
PGADMIN_DEFAULT_PASSWORD=your_secure_password_here

# Security Note:
# - Use strong passwords in production
# - Never commit the actual .env file to version control
# - The bootstrap.sh script can generate strong passwords for you